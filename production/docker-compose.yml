version: '3.2'

services:
  db:
    image: postgres:13.1
    privileged: true
    env_file:
      - .db-postgres.env

  backend:
    build:
      context: ./backend
    depends_on:
      - db
    expose:
      - 8081
    command: bash -c "
      python manage.py makemigrations &&
      python manage.py migrate &&
      gunicorn news_site.wsgi -b 0.0.0.0:8081
      "

  nginx:
    build:
      context: ./nginx
    ports:
      - 8000:80
    depends_on:
      - backend

  frontend:
    build:
      context: ./frontend
    ports:
      - 85:80
    depends_on:
      - nginx